# Included at server level AND in any location with its own add_header, since
# nginx drops inherited add_header once a context defines one of its own.
add_header Alt-Svc 'h3=":443"; ma=604800' always;
add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
