[Unit]
Description=Dead-man switch: reboot if ssh.alvarezrosa.com is unreachable
After=network-online.target wg-quick@wg0.service
Wants=network-online.target

[Service]
Type=oneshot
ExecStart=/bin/sh -c 'for i in 1 2 3; do nc -z -w20 ssh.alvarezrosa.com 22 && exit 0; sleep 10; done; /sbin/reboot'
