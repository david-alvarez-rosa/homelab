[Service]
# Creates /var/cache/unwall owned by the service user (the process cannot mkdir
# under /var/cache itself). This is the SSD tier behind the in-RAM LRUs.
CacheDirectory=unwall
CacheDirectoryMode=0750
